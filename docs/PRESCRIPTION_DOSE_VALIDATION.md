# Prescription dose validation

Each selected dose must exactly match an authoritative candidate profile after applying only supplied pace, load or percentage interfaces. Counts and durations must be positive, ranges must be valid, minimum rest must hold, and candidate intensity floors apply. A fresh Phase C evaluation verifies caps and recovery requirements. Phase E rejects violations; it does not lower intensity, remove work or substitute content.
